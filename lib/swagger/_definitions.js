/**
 * @typedef {import('openapi-types').OpenAPIV2.SchemaObject} Schema
 */

/**@type {Schema} */
export const Hello = {
    description: '"Hello, world" padrão',
    type: 'string',
    enum: ['Hello, world']
};
